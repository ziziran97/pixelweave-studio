# AI 协作约定

适用于本仓库的代码、文档和本地验证。本文记录稳定的协作方式与已确认的产品基线；功能细节、接口契约和启动说明见 [README.md](./README.md)。

## 开始任务

1. 先读本文、README 和 `git status --short`，了解项目边界及已有未提交修改，再阅读本次涉及的最新代码。
2. 使用中文交流。用户说“我们交流下”“先看看”“有什么建议”时，先检查、分析和讨论，不修改项目文件；明确说“帮我修改”“帮我优化”“帮我沉淀”后，完成已授权的本地工作和必要验证，不重复索要确认。
3. 以用户最新明确要求为先；判断现状以当前代码和实际界面为依据。已确认的交互规则是默认修改基线，发现实现偏差应说明并按任务范围处理，不能把现有缺陷当成设计要求。
4. M-37218 PRD 主要参考业务思路，可能滞后于本项目。不要为对齐旧 PRD 擅自撤回已确认能力，例如当前保留的调色。影响业务范围或交互含义的冲突先说明，不自行扩大需求。
5. 本文不是不可变的限制。用户明确提出新需求时可以调整基线，并在该次修改中同步受影响的约定。

## 业务与产品原则

- 项目服务商品图片审核中的轻量二改。独立页面用于开发和预览，最终检测、保存和审核回流由宿主系统接入。
- 画布只有一张当前底图，新增文字、矩形、椭圆和画笔保持独立、可编辑。优先保证修图效率、结果可靠与误操作恢复。
- 常用操作直接可见，次要设置和帮助按需展开；提示文案简洁，说明操作及结果。空格等隐藏交互应在相关位置提供简短提示。
- 图标沿用 `lucide-react` 和已有按钮组件，提供中文可访问名称、悬停及键盘提示；选中、禁用、隐藏、锁定状态要清楚区分。
- 界面不暴露无助于用户决策的接口字段、技术细节。页面品牌文案和标题使用“自研图像编辑能力”。
- 未获本次需求确认，不主动增加分组、合并图层、专业排版、生成式图层等复杂能力，也不恢复已移除的入口或快捷键。

## 模块状态与修改范围

- 消除笔、绘制、调色、图层、选择／查看及通用交互已形成当前阶段的交互基线，后续默认保持操作流程与文案，优先修复问题；新的体验调整按用户明确提出的范围开展，不因继续优化文字而重新设计其他模块。
- 文字是下一阶段的优化重点，已有可用功能及已确认规则，整体体验尚未定稿。继续调整时以当前代码和已确认规则为起点，先明确场景、范围和取舍，再按用户授权修改；不要把现有实现当作不可调整的最终方案，也不自行撤回已确认能力。
- 优化某一模块不代表其他模块重新开放设计。涉及共享颜色面板、数值控件、画布、历史或图层时，保留其他模块的既有行为并验证相关流程。
- 前端交互形成基线不代表真实消除、文案／成图检测、持久化保存及审核回流已完成联调；服务接入与效果验收仍需分别落实。

## 已确认的交互基线

### 工具与查看

- 左侧为消除笔、绘制、文字、调色；左侧高亮表示工作区，底部高亮表示选择／平移模式，两者独立。
- 有新增内容时，切换到消除笔或调色直接进入工具，并沿用画布上方自动收起的提示说明仅处理底图；消除笔另说明新增内容暂时隐藏、退出恢复原有显示。无新增内容或重复点击当前工具不追加提示，不增加确认弹窗；未完成选区被取消时优先说明取消结果。
- 取消选择，以及删除、隐藏或锁定当前对象后，保留所属工作区，显示对应的新建样式，不退回空白“工具属性”。多选面板标题为“已选 X 个图层”，正文分为移动/缩放/旋转说明及单选编辑属性的次要提示；平移时改为返回选择的引导，不显示单个对象或新建样式，不重复放删除按钮。多选不另行改变工作区高亮或面板展开状态，单选后恢复对象属性。
- 进入编辑默认选中消除笔并展开左侧属性栏，初始无选区，不自动消除。之后使用工具或选中对象时按需展开；画布手势结束后才改变布局。取消选择不自动收起，用户可手动收起，再次点击工具或重新选择对象可展开；切换保持缩放、画布中心对应的图片位置和对象选择。快捷键集中在左下方“帮助”，各工具保留必要的操作提示；关闭帮助保留入口焦点，不自动重现提示，鼠标移开再移入或键盘重新聚焦后才显示。
- 选择、平移在画布底部，按住空格可以临时平移。绘制矩形选区时，松手前按住空格移动的是选框，应与画布平移的提示区分。
- 文字、画笔、矩形、椭圆和多选外框统一使用蓝色边框、白底蓝边控制点，移动时保持清晰；控制点尺寸及各对象已有调宽、缩放、旋转方式保持不变，选中辅助样式不进入成图。
- 选择／平移切换保留工作区、对象选择和视野。撤销／重做尽量保留仍存在且可编辑的选中对象，不能因此自动展开已收起的属性栏。粗细及调色滑块实时预览，一次连续调整记一步历史，松手、键盘调整结束或失焦时收尾；仅新建样式、工具和查看变化不记历史。
- 必要操作说明放在对应面板，区分新绘制和已有对象调整；普通操作反馈浮在画布上方并自动收起，错误及需处理事项持续显示，不通过增减提示条改变画布尺寸。消除笔内部流程与提示保持现状。
- 底部缩放区保留减号、当前比例、加号、100% 数字和适配图标一键入口；比例只读、宽度固定，不增加输入框、下拉或滑块。达到 3% / 400% 时禁用对应方向。
- 左上角依次为撤销、重做、还原初始、原图对比，使用图标。右下角保留原图对比入口，两处共享状态。
- 当前工作图片尺寸保留在右上角，格式为“宽 × 高 px”，使用无底色、无边框的辅助文字；上传换图后更新，缩放和原图对比不改写尺寸显示。窄窗口优先保留操作按钮。
- 原图对比为“按住查看原图，松开返回”，不是点击切换。鼠标、键盘、失焦和指针取消都需正确恢复；临时查看不改写草稿。
- 对比原图和还原初始以进入本次编辑时的图片为基准，上传新图也不改变该基准。还原初始需确认，并可撤销。

### 消除笔

- 消除笔前端交互已确认定稿；后续默认保持流程和文案，优先修复问题、完成后端联调。用户明确提出的调整仍可实施。
- 保留涂抹、矩形框选、自由圈选、多边形套索，以及添加、减去、撤销和清空。圈选松手自动闭合，套索逐点点击并在起点闭合；完成选区后由用户点击“开始消除”。
- “按住隐藏选区”只临时隐藏遮罩，不清空选区，也不等同于查看初始原图。不恢复已移除的 `[`、`]` 笔刷尺寸快捷键。
- 消除工作区切到选择／平移后，面板显示当前模式、“返回消除笔”入口及已保留的方式、选区操作、笔刷大小和选区状态摘要，不堆放禁用控件或重复提示。返回恢复完整设置与已完成选区，不自动消除；按住空格临时平移不切换该面板。
- 消除只处理当前工作底图；新增文字和绘制内容不进入消除源图，结果采用后仍保持独立可编辑。消除模式下临时只预览底图，不改写各图层的可见性。
- 结果先预览，由用户明确采用或放弃；采用支持撤销。取消、失败、预览失败和采用失败应保留相应草稿与重试条件；结果预览中的 Esc 不直接放弃结果。

### 绘制

- “绘制”统一包含画笔、矩形、椭圆。切换类型用于新绘制，不转换已有对象；选中已有对象后展示其属性。属性滚动时三种绘制入口保持可见，紧凑布局优先保留常用设置。
- 矩形、椭圆画完保持连续绘制，不选中新对象、不显示控制框；每个图形独立成层并可逐步撤销。通过底部选择或图层选中后编辑已有对象；在选择模式取消选择不自动开始绘制，再次点击类型才继续画。
- 图形直接切换实心／边框，颜色名称随之变化，三种绘制类型共用最近设置的颜色。边框通过带示意的下拉选择实线、虚线、密虚线、圆点线、点划线；悬停或方向键只浏览，确认选项后应用，Esc 只关闭菜单并返回入口焦点。
- 图形不透明度初始为 100%，支持滑块和数字输入。0% 仍保留可恢复的图层并标记“完全透明”，新建样式与已有对象分别提示；空心区域允许点选下方内容，边线保留点击容错，0% 图形不拦截画布点选。切换样式、复制和撤销保留这些属性。
- 画笔粗细、边框粗细和矩形圆角的名称与数字框、px 单位同一行，下方放滑块。连续拖动实时预览且记一步历史；数字输入以回车或失焦应用，Esc 取消输入、保留焦点，不记历史或关闭编辑。
- 拉伸保持面板指定的笔画粗细和矩形圆角，拖动预览与松手结果一致。圆角最多为当前短边的一半，已有矩形的输入与滑块上限同步，新建圆角预设在绘制完成时按尺寸限制；复制、历史恢复和成图保持一致。
- 绘制中辅助轮廓、细画笔定位点仅用于预览，不进入成图或取色；选中后沿用现有控制框。图形误触按屏幕拖动距离过滤，保留有效细长图形与画笔点击画圆点。画笔范围圈保持深浅对比；绘制过程中暂停滚轮缩放，松手恢复。

### 对象颜色与取色

- 文字、图形和画笔共用颜色控件，保留常用色、自定义和图片取色；与修改底图效果的“调色”工作区区分。自定义入口回显当前颜色；面板优先放在属性栏内，保留色板和色相，数值支持 HEX／RGB／HSL 切换，入口放在输入框左侧，默认 HEX。
- 格式切换只改变显示，输入有效值实时预览，应用记一步历史，取消、Esc 或点击外部还原且不操作画布；新建样式不记历史。不增加透明度或最近颜色。点击“原颜色”恢复本次打开面板时的颜色，可继续试色，应用后才确认。
- 取色带像素放大镜，按当前可见成图采样且排除辅助线，作用于开始时的对象或新建样式。弹窗内取色返回草稿，仍需应用；Esc 只退出取色，完整保留输入、格式及色板位置。

### 文字

- 点击“文字”进入工作区，通过面板“添加文字”在当前可见图片区域中创建并直接输入；画布点击只选择，不自动新增。
- 新文字优先居中，附近已有文字时自动错开，保持在当前可见图片区域；短时间误连点不重复创建。新增后选中并输入，图层列表按原规则定位，不自动展开已收起面板。
- 点击空白处结束输入并取消选择，保留文字工作区及面板展开/收起状态。选中文字显示“当前文字属性”，未选中显示“新文字样式”，只影响下一次新增；选中图形则展示图形属性。
- 保留常用文字样式与按需展开的更多设置，不增加文字模板或独立文案输入框。阴影未启用时仍记住该文字的阴影颜色，后续启用、复制和历史恢复保持一致。新增文字仍独立成层，可拖动、锁定、隐藏、删除和撤销。

### 调色

- 面板按基础调节、滤镜、颜色叠加排列，重置调色位于最后。基础调节仅亮度、对比度、饱和度、色温、锐化；色温是相对冷暖，不显示开尔文，锐化默认关闭。已移除整图模糊，不增加分色 HSL、高光、阴影、绿红色调或混合模式。
- 颜色叠加默认折叠、强度为 0%，收起时仍回显已启用的强度，复用颜色控件和取色；取消叠加仅归零强度。滤镜为八种效果加无滤镜，同时仅一个生效、强度可调；黑白、复古归入滤镜。切换滤镜保留其他调节及滤镜强度，无滤镜只取消滤镜。
- 所有效果作用于当前工作底图，不改变新增文字、图形和画笔对象的属性；选中新增对象也不切换调色的作用目标。固定顺序重新计算，不累计叠加；缩略图按当前底图生成小图，隔离过期响应。
- 调色实时预览，连续滑动记一步历史，数字回车/失焦应用、Esc 取消。叠加颜色应用记一步历史、取消恢复。重置调色清除整套参数且可撤销，不删除新增内容或撤回已采用消除结果。
- 当前效果进入消除源图和最终成图；采用消除结果后重置已烘焙参数，避免重复叠加，撤销采用恢复原底图、选区和参数。上传清空调色，原图对比基准保持不变。

### 图层

- 图层行展示类型图标、颜色、名称及显示/锁定按钮，底图使用实际图片缩略图。正常行不重复堆放说明，异常、隐藏和锁定状态按需展示。
- 底图固定在最下方，不参与普通图层的选择、排序、复制和删除；临时消除选区不加入图层列表。
- 隐藏图层不参与最终成图；锁定防止误编辑，不改变是否成图。点击名称不自动显示或解锁。
- 操作未选中的图层，不清空当前选择；多选中隐藏或锁定其中一层，只移除受影响的选择，保留其余对象及位置。
- 图层底部依次为置顶、上移一层、下移一层、置底，与复制、删除适当留白。排序仅针对单个可编辑图层，置底仍在底图上方；到顶或到底时禁用对应方向的两个按钮。排序一次记一步历史，保留选择和画布视野。复制、删除使用图层文案，多选时明确数量；删除可撤销，不额外增加确认弹窗。
- 面板默认展开，本次编辑内保留收起状态。切换工具、新增内容不自动展开；新增、复制和选择时仅滚动图层列表定位，不改变画布视野。
- 校验失败通过“查看问题图层”显式展开并定位，保留图层隐藏/锁定状态。面板收起和展开保持缩放、图片中心及对象选择。

### 上传、替换与确认

- 右上角“上传本地图片”仅载入编辑草稿；“替换图片”才将结果提交到任务，两者必须清楚区分。
- 上传成功后选中消除笔，保持左侧属性栏原有的展开或收起状态；取消或失败保持原工具与面板状态，不自动消除。
- 替换、还原、上传、关闭和切图沿用 `ConfirmationDialog` 的对应条件与文案，不新增 `window.confirm`。不要把所有可撤销操作都改为二次确认。
- 确认弹窗默认聚焦取消，Esc 和关闭按钮等同取消，点击遮罩不关闭；确认之前不发起对应检测或保存，避免重复执行和晚到结果抢占弹窗。
- 浏览器刷新、关闭标签页的离开提醒仍由原生 `beforeunload` 承担；结果预览和提交进度弹窗保留各自行为。

## 接口与数据边界

- 不将“前端已完成”“配置了接口地址”“固定样图测试通过”描述为真实 AI 消除或业务保存已联调成功。缺少服务时保留草稿/选区并明确提示，不伪造成功结果。
- 消除适配器当前提交 `image.jpg`、`mask.png` 和 `metadata`。源图是当前工作底图，不一定等于进入编辑时的初始原图；Mask 与源图同尺寸，为不透明黑白 PNG，白色修改、黑色保留。字段和响应细节以 README、`src/lib/eraseApi.ts` 为准，仍需后端确认。
- 当前只采用与输入同尺寸的结果，不擅自缩放结果或重排新增内容；模型生成质量、选区外像素保持及商品一致性需要真实联调验收。
- 保留请求取消、文档身份/版本校验和晚到响应隔离。换图、取消、卸载后，旧结果不能覆盖当前文档；停止前端等待不等于后端任务已停止。
- 宿主契约见 `src/integration.ts`。隐藏图层不成图，但其仍保留的非空新增文字仍纳入文案校验，不混淆这两条规则。
- 保存发出后超时属于结果未知，沿用同一 `submissionId` 查询，防止重复保存；已保存但审核刷新失败时只重试回流，不再次保存。
- 本地撤销历史不是持久化工程或服务器草稿。服务端密钥不得写入前端环境变量、代码、日志或 Git；已有 `.env` 不覆盖。

## 修改入口与验证

技术栈为 React、TypeScript、Fabric.js、Vite，具体版本看 `package.json`。优先沿用现有组件和状态管理，不为局部优化整体重写。

| 修改内容 | 主要入口 |
| --- | --- |
| 页面布局、工具分组、样式 | `src/App.tsx`、`src/styles.css` |
| 工具面板、图层、按钮与弹窗 | `src/components/` |
| 画布、历史、选择、任务生命周期 | `src/editor/EditorController.ts`、`src/editor/SelectionGesture.ts`、`src/editor/model.ts` |
| 图像资源、Mask、成图 | `src/editor/assets.ts`、`src/editor/mask.ts`、`src/editor/render.ts` |
| 消除接口、宿主保存契约 | `src/lib/eraseApi.ts`、`src/integration.ts`、`src/types.ts` |

- 本地启动、环境变量按 README；需要固定端口时使用 `npm run dev -- --port 4175`，以实际启动输出为准。已有可用依赖和服务时直接复用。
- 代码变更运行 `npm run build`，涉及文档模型、历史、Mask 等基础规则时运行 `npm test`；UI 和交互变化还需运行对应浏览器检查，不能只凭构建成功判断交互正确。
- 开发服务上的验证入口：`/tests/browser.html`（编辑、像素、选区、请求及替换契约）、`/tests/eraser-ui.html`（消除笔及结果预览）、`/tests/controls-ui.html`（按钮、对比和面板）、`/tests/drawing-ui.html`（绘制工作区、模式、提示及连续属性调整）、`/tests/color-ui.html`（颜色预览、取消及取色）、`/tests/adjustments-ui.html`（基础调节、叠加、滤镜、历史与布局）、`/tests/layers-ui.html`（图层）、`/tests/confirmation-ui.html`（确认弹窗）。
- 影响共享控制器或组件时覆盖相关流程；修复选择丢失、状态恢复、异步回写等问题时补充有意义的回归用例。测试通过后，除非又有修改或发现问题，不反复扩大测试范围。
- 视觉变化检查实际页面，关注紧凑布局、文字截断、点击范围、提示裁切和键盘操作。无法执行的验证明确说明，不沿用旧对话的通过记录。
- 纯文档修改核对事实、路径、命令和差异即可，不强制运行无关测试。收尾检查 `git diff --check`，说明本次变化、验证结果及实际限制。

## Git 与文档维护

- 保留现有未提交修改，不回滚、覆盖、清理或暂存与本次任务无关的内容。
- 本地修改授权不等于 GitHub 发布授权。提交、推送或发布需要用户另行明确授权；已获授权的范围内直接完成，不反复询问。需要新建分支时使用 `codex/` 前缀。
- 发布时只包含授权范围内的文件，并核对差异和远端结果；没有实际发布与验证，不声称线上已更新。
- AGENTS.md 保持精简，记录协作约定和稳定基线；README 记录现有能力、接口与使用方式。用户确认的规则发生变化时同步相关文档，不在这里追加聊天流水、临时计划、历史测试数量或机器专属路径。
