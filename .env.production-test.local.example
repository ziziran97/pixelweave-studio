# Copy to .env.production-test.local; only the local Node server reads these.
# Fill both values yourself, then restart npm run dev:production-test.
# Use quotes around values; escape a literal dollar sign as \$.
LAMA_USERNAME=
LAMA_API_KEY=
